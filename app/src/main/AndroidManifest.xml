<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="ch.epfl.sweng.groupup">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activity.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.login.LoginActivity"/>
        <activity android:name=".activity.info.UserInformationActivity"/>
        <activity android:name=".activity.event.listing.EventListingActivity"/>
        <activity android:name=".activity.event.creation.EventCreationActivity"/>
        <activity android:name=".activity.info.DisplayQRActivity"/>
        <activity android:name=".activity.settings.SettingsActivity"/>
        <activity android:name=".activity.toolbar.ToolbarActivity"/>
        <activity android:name=".activity.event.description.EventDescriptionActivity"/>
        <activity android:name=".activity.event.creation.MembersAddingActivity">
        </activity>
    </application>

</manifest>
